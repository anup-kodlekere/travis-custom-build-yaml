jobs:
  include:
    - stage: "Tests"                
      name: "Unit Tests"
      arch: ppc64le
      script: echo "unit"
    - stage: deploy
      name: "Deploy to GCP"
      arch: ppc64le
      script: echo "deploy"
